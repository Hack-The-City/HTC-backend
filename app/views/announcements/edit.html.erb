<%= form_for @announcement do |f| %>
  <%= render "form", f: f %>
<% end %>

<%= link_to "Delete", announcement_path, method: :delete,
  "data-confirm" => "Are you sure you want to delete this?"
  %>
